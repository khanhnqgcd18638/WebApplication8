@model List<WebApplication8.Models.ApplicationUser>
@{
    ViewBag.Title = "StaffView";
}

<h2>Staff View</h2>

@Html.ActionLink("Create a new account", "Register", "Account", null, new { @class = "btn btn-primary" })
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">User Name</th>
            <th scope="col">Email</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.UserName</td>
                <td>@item.Email</td>
                <td>
                    @Html.ActionLink("Update", "UpdateStaff", "Admin",
                                 new { id = item.Id },
                                 new { @class = "btn btn-warning" })
                    @Html.ActionLink("Delete", "Delete", "Admin",
                                new { id = item.Id },
                                new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>

